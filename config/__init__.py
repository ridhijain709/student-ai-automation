"""
Vertical-specific automation rules for demo + client deployments.

This package is intentionally lightweight: each vertical exposes a single
`WHATSAPP_TRIAGE_PROMPT` string and a `CTA_HINTS` list to keep copy consistent.
"""

